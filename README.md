# RealQM-Gemini-Segre_Chart

Vectorized parallel MatrixMechanics engine automating L-BFGS-B orientation relaxation across all 582 isotopes of the Segrè stability valley on consumer-grade laptop hardware (ASUS ROG Strix 18 G814PP).

## 🏛️ Project Framework & Anniversary Milestone
This repository houses the final, generalized codebase of the **RealQM Strategic Modeling Program**, marking the **200th landmark research publication** by author Jean Louis Van Belle. 

By shifting from traditional, continuous multi-body Schrödinger partial differential wavefunctions to localized, phase-locked *Zitterbewegung* current loop networks, the nature of nuclear configuration space collapses into a discrete topological matrix. This package replaces high-performance supercomputing cluster demands with sub-second parallel NumPy broadcasting array operations, sweeping the entire periodic table from Hydrogen ($Z=1$) up to Oganesson ($Z=118$) in a single automated session.

## 🧪 Core System Architecture
The initialization of any arbitrary nuclide follows a strict, coordinate-free **Nested Core-Satellite Hierarchy Rule**:
1. **Primal Alpha Tetrads ($N_{\alpha}$)**: Nucleons factorize into alternating, face-paired regular tetrahedrons to achieve perfect near-field magnetic induction and geometric torque cancellation ($\mathbf{\tau} = \mathbf{0}$).
2. **Anchor Core ($N \le 41$)**: The inner double-magic foundation layer locks up to 41 alpha units (Lead-208 horizon) into a hyper-stable central sphere.
3. **Polar Caps**: Remainder alpha units push to the axial boundaries ($\pm 5.0$ fm on the $Z$-axis) via central electrostatic Coulomb repulsion, natively mapping out pre-formed fission corridors.
4. **Core Buffers ($N \le 36$)**: Excess neutron sub-loops populate an intermediate insulation radius ($2.7$ fm) to mask proton-proton contact points.
5. **Fringe Satellites**: Overflow valence neutrons occupy the far macroscopic margin ($6.8$ fm) to act as a phase blanket, damping long-range relativistic phase delays ($\cos(r_{ij}/\lambda_C)$).

## 🚀 Execution Guide
Ensure you have Python 3.10+ installed with `numpy` and `scipy`. Execute the global relaxation scan directly from your terminal console:

```bash
python global_relaxed_scan.py
```

## 📂 Module Files Matrix
* `periodic_table_solver.py`: A lightweight, localized testing loop tracking key structural milestones (Silicon, Calcium, Iron, Uranium).
* `global_relaxed_scan.py`: The supercharged periodic table sweeper embedding an active L-BFGS-B optimization routine to resolve the true ground-state valley of stability.
* `global_relaxed_valley_telemetry.csv`: The final, compiled database mapping out relaxed Graph Laplacian Fiedler stiffness values and spectral radii across 582 discrete nuclide states.

***
*Celebrating two decades of decentralized, first-principles alternative quantum and nuclear physics.*
